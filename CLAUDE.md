# DEBATE ARENA — Hệ thống tranh luận có cấu trúc về chiến tranh Nga–Ukraine

## Mục đích
Tạo một cuộc tranh luận chất lượng cao giữa hai "chuyên gia" đại diện cho hai khung lập luận đối lập về cuộc chiến Nga–Ukraine, trên 4 trục: **tính chính đáng (just war theory), lịch sử, luật quốc tế, logic**. Kết thúc bằng một bảng điểm định lượng do Judge chấm theo rubric có neo điểm.

## Nguyên tắc nền tảng (KHÔNG ĐƯỢC VI PHẠM — áp cho MỌI vai trò, kể cả subagent)
1. **Chấm chất lượng lập luận, không phán quyết chân lý.** Điểm số đo "bên nào lập luận chặt chẽ hơn theo rubric", không phải "bên nào đúng về mặt đạo đức/lịch sử tuyệt đối". Mọi báo cáo cuối phải ghi rõ điều này.
2. **Đối xứng tuyệt đối.** Hai advocate dùng prompt giống hệt nhau (chỉ khác tham số POSITION). Ngân sách độ dài mỗi vòng bằng nhau (±10%). Thứ tự phát biểu luân phiên qua các vòng.
3. **Steelman bắt buộc.** Mỗi advocate phải trình bày phiên bản mạnh nhất của lập luận phía mình VÀ trước khi phản biện phải tóm tắt trung thực luận điểm đối phương.
4. **Chống bịa đặt (anti-fabrication):**
   - Mọi khẳng định sự kiện phải kèm nguồn: `[Nguồn: tên, năm]` hoặc trích từ `knowledge/case_file.md`.
   - Nếu không chắc về một sự kiện/văn kiện → ghi `[CẦN KIỂM CHỨNG]`, không được viết như sự thật.
   - Cấm bịa số liệu thương vong, ngày tháng, điều khoản hiệp ước, phán quyết tòa án.
   - Văn kiện pháp lý phải dẫn đúng số điều (vd: Điều 2(4) Hiến chương LHQ), nếu không nhớ chính xác → tra cứu hoặc đánh dấu.
5. **Không tuyên truyền.** Đây là phân tích học thuật. Không dùng ngôn ngữ phi nhân hóa, không cổ vũ bạo lực, không phủ nhận tội ác đã được tòa án/điều tra quốc tế xác lập.

## Harness: debate-arena

**Trigger:** mọi lệnh vận hành debate — `START PHASE 0`, `START DEBATE`, `START JUDGING`, `FULL RUN`, `EXTRA ROUND [chủ đề]`, `SWAP TEST`, `NOISE TEST`, `APPROVE CASE FILE`, `APPROVE CASE FILE ADDENDUM`, `APPROVE TRANSCRIPT` — hoặc yêu cầu tương đương bằng lời thường (chạy/chạy lại phiên, thêm vòng, chấm lại, cập nhật case file, đo nhiễu judge, audit steelman) → dùng skill **`debate-orchestrator`**. Câu hỏi thông thường về nội dung file thì trả lời trực tiếp.

**Kiến trúc:** orchestrator là vai duy nhất trong context chính; advocate / fact-checker / judge / steelman-auditor chạy bằng subagent (`.claude/agents/debate-*.md`, model opus) với context được nạp riêng từng vai VÀ `tools:` bị giới hạn trong frontmatter để subagent không tự đọc được file ngoài danh sách nạp — hai tầng này là cơ chế thực thi Quy tắc thông tin trong `protocol/debate_protocol.md`. Khung chấm theo chủ đề nằm ở `knowledge/judge_notes.md` — CHỈ nạp cho judge, KHÔNG BAO GIỜ cho advocate/fact-checker/auditor; không thêm nội dung "bên X được điểm/bị trừ" vào case file. Chi tiết quy trình, bảng nạp context, xử lý lỗi: xem skill.

**Quy tắc bất biến ngoài skill:**
- Ngôn ngữ output: **tiếng Việt** (thuật ngữ pháp lý kèm tiếng Anh trong ngoặc lần đầu xuất hiện).
- Judge không bao giờ phát biểu trong Phase 1. Advocate không bao giờ tự chấm điểm.
- Phán quyết chính thức lấy từ **hội đồng 3 judge độc lập, tính trung vị**. Chênh trung vị ≤5% tổng điểm HOẶC khoảng điểm hai bên chồng lấn → "không phân định được theo rubric này", KHÔNG ép ra người thắng.
- Hai gate người duyệt là bắt buộc, không tự động vượt.
- **Bộ chủ đề:** harness chạy nhiều chủ đề bằng các bộ file cùng hậu tố (mặc định Nga–Ukraine; `_tuyenquang`; `_gochoa`). Chọn bộ TRƯỚC khi nạp context cho bất kỳ subagent nào; **không trộn file giữa hai bộ** — nạp nhầm `judge_notes` của chủ đề khác là lệch neo im lặng. Chủ đề **ĐANG DIỄN RA** (dữ kiện còn đổi ngoài đời thực) áp thêm quy tắc đóng băng dữ kiện trong `protocol/debate_protocol.md`.
- Báo cáo cuối phải khai báo giới hạn: hai advocate và judge chạy cùng một model (prior có thể làm steelman một bên yếu hệ thống); SWAP TEST chỉ bắt thiên lệch nhãn, không bắt thiên lệch nội dung.

**Change log:**
| Ngày | Thay đổi | Đối tượng | Lý do |
|------|----------|-----------|-------|
| 2026-06-12 | Xây harness ban đầu (agents/, rubrics/, protocol/, knowledge/, templates/) | toàn bộ | — |
| 2026-06-12→13 | Chạy 2 phiên debate + EXTRA ROUND (E4) + case file §10–14 | output/, case_file | vận hành thực tế |
| 2026-07-04 | Vá quy tắc chấm: ô N/A không vào trung bình, bỏ phạt kép cờ đỏ, quy tắc EXTRA ROUND, hợp thức hóa rubric thấu kính thay thế | rubrics/, agents/judge.md | audit 2026-07-04 |
| 2026-07-04 | Cách ly vai trò bằng subagent: `.claude/agents/debate-{advocate,judge,fact-checker}.md` + skill `debate-orchestrator`; SWAP TEST bằng instance judge mới | .claude/, protocol/ | Quy tắc thông tin trước đây không có cơ chế thực thi |
| 2026-07-04 | Script đếm từ (`word_count.ps1`), quy ước hậu tố file `_vN`, ghi ngày duyệt gate vào transcript, thư mục `_workspace/` | skill, protocol/ | ngân sách từ ±10% trước đây không đo được |
| 2026-07-12 | Vá rò rỉ rubric: tách các mục "Khung Judge" (§8.3, §9.5, §10.4, §11.4, §12.4, §14.3) khỏi case file sang `knowledge/judge_notes.md` (chỉ nạp cho judge); siết cách ly bằng `tools:` frontmatter (advocate/fact-checker: WebSearch+WebFetch; judge: Write) | knowledge/, .claude/agents/, skill, protocol/ | audit 2026-07-12 (P0): case file nạp cho advocate chứa tiêu chí chấm điểm; cách ly trước đây vẫn dựa một phần vào lời dặn |
| 2026-07-12 | Hội đồng 3 judge lấy trung vị + bảng hội đồng trong scorecard; lệnh `NOISE TEST` đo nhiễu nền judge; ngưỡng "không phân định" định nghĩa lại theo trung vị/chồng lấn | skill, rubrics/, agents/judge.md, templates/ | audit 2026-07-12 (P0): ngưỡng 0.5 trước đây chưa hiệu chuẩn với nhiễu lấy mẫu của judge đơn lẻ |
| 2026-07-12 | Steelman audit bắt buộc trước GATE 2 (agent `debate-auditor`, chạy đối xứng 2 bên, vào phụ lục scorecard); khai báo giới hạn "thiên lệch prior cùng model" + "SWAP TEST chỉ bắt thiên lệch nhãn" vào template scorecard | .claude/agents/, skill, protocol/, templates/ | audit 2026-07-12 (P1): SWAP TEST không phát hiện được thiên lệch nội dung; chưa có phép đo trần steelman |
| 2026-07-12 | Script `quote_check.{sh,ps1}` đối chiếu trích dẫn scorecard với transcript + quy ước "ngoặc kép chỉ dành cho trích nguyên văn"; ghim case file bằng git hash tại GATE 1 + lệnh `APPROVE CASE FILE ADDENDUM`; tổng kép V1–V5 / V1–V5+E khi có vòng E (đảo hạng → không phân định); metadata model + ngày vào header transcript/scorecard | skill scripts/, protocol/, rubrics/, agents/judge.md, templates/ | audit 2026-07-12 (P2): kiểm thử trên scorecard v2 cho thấy 27/34 "trích dẫn" của judge không khớp nguyên văn transcript; case file từng bị sửa sau GATE 1 không gate lại; vòng E là vector thiên lệch chọn chủ đề; đổi model làm mất tính so sánh giữa phiên |
| 2026-07-12 | Thước đo ngân sách từ loại tag trích nguồn inline + danh sách nguồn cuối lượt khỏi phép đếm (`word_count.{sh,ps1}`); quy tắc chống né fact-check trong rubric chiều 2 (lượt mở đầu/kết luận gần như không có khẳng định kiểm chứng được → trần neo 4); sửa đường dẫn README | skill scripts/, protocol/, rubrics/, README | audit 2026-07-12 (P3): thước đo cũ tạo động cơ cắt nguồn để tiết kiệm từ; "không lỗi nguồn nhờ không nói gì kiểm chứng được" trước đây không bị neo nào chặn |
| 2026-07-13 | Vá sau NOISE TEST đầu tiên: case file RÚT GỌN cho judge (giữ §2/§3 + § được transcript trích; các vai khác vẫn nhận đầy đủ); quy trình nạp nhiều phần + resume subagent sau gián đoạn hạ tầng vào skill; `quote_check` lowercase ASCII (hết false positive hoa/thường đầu câu); bảng hội đồng thêm trung vị theo chiều; mốc hiệu chuẩn nhiễu nền 0.2–0.4 vào protocol; banner "chưa tái thẩm" cho scorecard phiên 1; xóa file trùng lặp | skill, scripts/, protocol/, templates/, output/ | vận hành NOISE TEST 12–13/07: nạp ~50k token ×3 instance làm phiên chạm session limit 2 lần; trích giữa câu bị cờ oan vì chữ hoa đầu câu; hội đồng chỉ gom tổng làm mất thông tin chiều nào gây khoảng cách |
| 2026-07-14 | Mở rộng harness cho chủ đề mới (chính sách giáo dục nội địa) qua bộ file topic-scoped, KHÔNG đè bản Nga–Ukraine: `knowledge/case_file_tuyenquang.md`, `knowledge/judge_notes_tuyenquang.md`, `rubrics/scoring_rubric_tuyenquang.md` (rubric thấu kính thay thế — thay 2 chiều just-war/luật quốc tế bằng Công bằng & quyền lợi + Pháp lý & quy chế thi), `agents/advocate_template_tuyenquang.md` + `agents/position_{A,B}_tuyenquang.md` | knowledge/, rubrics/, agents/, output/ | phiên tranh biện vụ Tuyên Quang 2026 |
| 2026-07-14 | Vá lỗ hổng quote_check: **transcript nạp cho judge phải NGUYÊN VĂN, cấm rút gọn** (chỉ case file mới được rút gọn); ghi rõ trong bảng nạp context + đoạn quy tắc mới trong skill | skill | audit 14/07: orchestrator rút gọn transcript cho judge để tiết kiệm token → 3/3 scorecard quote_check FAIL 7–17 MISSING oan (trích lệch chuỗi so với file) dù lập luận đều có thật; phép kiểm chống-bịa-trích bị vô hiệu |
| 2026-07-15 | **P0-1 quote_check cơ giới hóa:** viết lại `scripts/quote_check.py` (casefold Unicode, chuẩn hóa dấu câu –/—/"/'/…, trích đa dòng, tách theo ellipsis, min 4 từ, đếm ô dùng ngoặc kép); `quote_check.sh` thành wrapper ưu tiên python3 (fallback awk); siết chỉ thị judge (`agents/judge.md` + `.claude/agents/debate-judge.md`): **ngoặc kép CHỈ cho trích nguyên văn, mọi câu phân tích của judge phải in nghiêng**, kèm ví dụ CẤM (phân tích trong ngoặc, trích ghép, sửa trích) + tự kiểm trước khi xuất; chạy quote_check trên MỌI lô (hội đồng/NOISE/SWAP/E) | scripts/, agents/judge.md, .claude/agents/, skill, protocol/ | audit Fable5 14–15/07: đặt câu phân tích của judge vào ngoặc kép là nguyên nhân FAIL phổ biến nhất; script cũ (awk lowercase ASCII) vẫn cờ oan trích đa dòng/dấu câu Unicode |
| 2026-07-15 | **P0-2 chống lệch điểm do transcript rút gọn:** thêm `scripts/make_judge_input.sh` (trích NGUYÊN VĂN V1→hết vòng cuối bằng awk ra `_judge_input.md`, orchestrator dán y nguyên, **cấm gõ lại/paraphrase**); quy tắc trong skill+protocol: trích không khớp → **nghi lỗi NẠP trước** (kiểm bản nạp nguyên văn) rồi mới trả judge sửa | scripts/, skill, protocol/ | NOISE TEST 14/07: transcript rút gọn nạp cho hội đồng làm yếu bên A ~0.46 điểm (hệ thống, vượt nhiễu nền 0.31) → thổi chênh B−A từ ~0.1 lên 0.7 (artifact vận hành, không phải tín hiệu thực) |
| 2026-07-15 | **P1 điểm chính thức = V1–V5:** rubric + protocol đổi "điểm chính thức = V1–V5+E" → **V1–V5** (chống thiên lệch chọn đề); vòng E vào phán quyết CHÍNH THỨC chỉ khi (a) chủ đề đối xứng (mỗi bên/auditor đề cử) VÀ (b) qua `APPROVE TRANSCRIPT ADDENDUM`; vòng E do người dùng chọn 1 đề = **chẩn đoán** (vào báo cáo kép, không đổi phán quyết); **fact-check vòng E bắt buộc** trước khi E vào bất kỳ tổng nào; **ước lượng biên** (chấm E riêng ghép nền lô khác) chỉ dùng chẩn đoán, không đổi phán quyết khi chênh trong nhiễu | rubrics/, protocol/, skill | audit Fable5: vòng E do người dùng chọn đề (sau audit) là vector thiên lệch; ghép biên trộn hai quần thể judge |
| 2026-07-15 | **P2 vệ sinh hồ sơ:** quy ước banner `⛔ SUPERSEDED — xem §X` khi một mục scorecard bị mục sau lật (không xóa, giữ dấu vết) vào `templates/scorecard_template.md`; hàng "Phạm vi fact-check" trong bảng Phạt (0 cờ đỏ chỉ áp vòng đã phủ); sửa artifact phiên 14/07: banner SUPERSEDED §5a scorecard, nhãn phạm vi "V1–V5" cho Phụ lục A transcript (vòng E/E2 chưa fact-check đầy đủ) | templates/, output/ | audit Fable5: §5a scorecard vẫn trình chênh 0.7 như hợp lệ dù §6 đã lật; Phụ lục A gắn nhãn "toàn transcript" trong khi vòng E/E2 chưa kiểm |
| 2026-08-07 | Vá trôi dạt tài liệu sau P1 + guard heading transcript: (1) scorecard template — bảng tổng kép đảo nhãn "chính thức" về **V1–V5** (trước đó vẫn gắn cho V1–V5+E, ngược bản vá P1 15/07); (2) transcript template codify heading `## Phụ lục A/B/C` (load-bearing cho `make_judge_input.sh`) + script fail to khi thiếu marker `## Vòng 1` hoặc khi phụ lục steelman audit sẽ lọt vào input judge (heading B thiếu/sai tên/đặt sai chỗ — chỉ bắt cụm "steelman audit"/heading, không bắt từ "steelman" đơn lẻ vốn hợp lệ trong lượt advocate); (3) judge đơn lẻ KHÔNG tự tuyên phán định thắng/thua/"không phân định" — chỉ báo tổng của mình, phán định là của orchestrator theo trung vị hội đồng (sửa `agents/judge.md` + đánh dấu dòng phán định §6 scorecard template là "orchestrator điền"); (4) cross-ref quy tắc P1 vào mục EXTRA ROUND của protocol + README | templates/, agents/judge.md, protocol/, README, skill scripts/ | audit 2026-08-07: template scorecard vô hiệu hóa im lặng bản vá P1; heading Phụ lục B chưa được template định nghĩa → audit có thể lọt im lặng vào input judge (vi phạm Quy tắc thông tin không có guard); judge.md mâu thuẫn rubric ("Từng judge KHÔNG tự áp mục hội đồng") |
| 2026-08-16 | **Bộ chủ đề thứ ba `_gochoa`** (đặt hoa tại gốc cây 55 Nguyễn Huy Tự): `knowledge/case_file_gochoa.md` (thêm mục §5 "dữ kiện đang tranh chấp/chưa kiểm chứng" — cấm dùng làm tiền đề theo CẢ HAI chiều; quy tắc **H4 đối xứng** — truyền thông vận động ở cả hai đầu quang phổ đều chỉ chứng minh "thông điệp đã phát", không chứng minh sự kiện), `knowledge/judge_notes_gochoa.md`, `rubrics/scoring_rubric_gochoa.md` (thấu kính thay thế: Chính đáng–tương xứng–**phân bổ rủi ro** thay just-war; Pháp lý & quyền hiến định thay luật quốc tế; thêm thang **neo cho khẳng định DỰ ĐOÁN** — có căn cứ / có cơ chế / suy diễn tự tin / xác suất giả), `agents/advocate_template_gochoa.md` + `agents/position_{A,B}_gochoa.md` (đề bài khóa B vào "chỉ khác về phương tiện, cùng mục tiêu" để cả hai bên đều biện hộ được trong thiện chí) | knowledge/, rubrics/, agents/ | chủ đề mới; đề bài "lành mạnh vs làm màu" bị loại vì gộp hai mệnh đề độc lập vào một phía và ép B vào vị trí không advocate trung thực nào giữ được (vi phạm Nguyên tắc 2–3) |
| 2026-08-16 | **Xác minh §5.1 bộ `_gochoa` + cơ chế HAI HẠNG cho dữ kiện chưa chắc:** thay lệnh cấm nhị phân bằng **hạng B** (đạt ≥2 nguồn H3 độc lập, không có H1/H2 → dùng được nhưng bắt buộc kèm tên nguồn + caveat "chưa có xác nhận chính thức") và **hạng C** (chưa đạt ngưỡng → vẫn cấm làm tiền đề). §5.1 (quan hệ hôn nhân Nguyễn Sỹ Cương – Lâm Thị Phương Thanh) nâng C→B; nhân thân + chức vụ Bộ trưởng VHTTDL của bà Thanh xác minh mức H1. Ba cảnh báo phương pháp mới: vắng mặt trong tiểu sử chính thức KHÔNG phải bằng chứng phủ định (tiểu sử cán bộ VN hệ thống không ghi vợ/chồng); **quan hệ ≠ can thiệp** (hạng B không kéo hạng cho suy luận dựng trên nó); **mệnh đề cấu trúc ≠ mệnh đề hành vi**. Kèm: §4 nâng hạng thông báo hạn chế Threads (bằng chứng nền tảng, khác hạng với tin đồn); §3 ghi hai con số lan truyền mâu thuẫn + quy tắc chống cherry-picking | knowledge/, rubrics/, agents/ | tra cứu theo yêu cầu người dùng: lệnh cấm nhị phân làm mất một dữ kiện đã đạt ngưỡng `source_policy.md`, nhưng cho dùng tự do thì mở đường cho bước nhảy quan hệ→can thiệp |
| 2026-08-16 | **Bản vá "sự kiện ĐANG DIỄN RA"** (harness trước đây chỉ chạy hồ sơ đóng): phân loại ĐÓNG/ĐANG DIỄN RA tại Phase 0; **mốc đóng băng dữ kiện = thời điểm GATE 1**; **advocate cấm dùng WebSearch để cập nhật tin** (chỉ xác minh nguồn cho dữ kiện đã có trong case file); cờ fact-check mới **⏱️ SAU MỐC ĐÓNG BĂNG**; addendum phải ghi **từ vòng nào advocate được biết**; quy tắc chấm "không phạt vì không biết tương lai / không thưởng vì biết tương lai"; **điều kiện ĐÓNG PHIÊN SỚM** khi diễn biến lật tiền đề đề bài (không vá bằng addendum); nhãn phạm vi thời gian + khai báo "không phải khuyến nghị hành động cho cá nhân" vào template; **`KEEP_EXTRA` cho `make_case_file_input.sh`** (các § judge cần để áp neo nhưng advocate không bao giờ trích tag — không có nó, §5/§8 của bộ `_gochoa` bị stub mất, đã kiểm chứng) | protocol/, skill, skill scripts/, templates/, README, CLAUDE.md | `tools:` chặn được đọc file cục bộ nhưng KHÔNG chặn advocate tra tin mới → với chủ đề đang diễn ra, advocate vòng 5 đứng trên nền dữ kiện khác advocate vòng 1 (phá đối xứng, không script nào bắt được); judge có xu hướng trừ điểm lượt đầu vì "không nhắc tới X" mà X chưa xảy ra lúc đó |
| 2026-08-07 | Cơ giới hóa nốt các khâu soạn tay còn lại: (5) `scripts/make_case_file_input.sh` — sinh bản case file RÚT GỌN cho judge bằng máy (quét tag `[Case file §x]` trong `_judge_input.md`, § con quy về § cấp 1; luôn giữ header + §2/§3; § không trích → stub; in sẵn dòng metadata + khuyến cáo "lược ≤1 § → dán đầy đủ"); CẤM soạn tay bản rút gọn (skill + protocol); (6) `quote_check.py` guard ghép cặp ngoặc lẻ — trích >400 ký tự hoặc chứa dòng trống → `SUSPECT-PAIRING` (kiểm tay, không tính MISSING/khớp), thêm chỉ số `suspect_pairing` vào dòng tổng kết; (7) skill Phase 0 thêm bước **chọn bộ file chủ đề** bắt buộc (mặc định Nga–Ukraine vs bộ topic-scoped `_<topic>`; cấm trộn bộ — đặc biệt judge_notes; ghi `bộ chủ đề:` vào metadata transcript/scorecard) | skill, skill scripts/, protocol/ | audit 2026-08-07 (gợi ý 5–7): bài học P0-2 (soạn tay = paraphrase = lệch điểm hệ thống) áp dụng y nguyên cho case file rút gọn nhưng khâu này vẫn soạn tay; một dấu `"` lẻ làm lệch mọi cặp ngoặc phía sau → MISSING oan hàng loạt kiểu mới; harness đã có 2 chủ đề nhưng skill hard-code bộ Nga–Ukraine, nạp nhầm judge_notes chủ đề khác là lệch neo im lặng |
